id: WI-002
type: work_item
title: Create _base.json stack with universal defaults and deny list
status: pending
complexity: medium
scope: []
depends: []
blocks:
  - "007"
  - "008"
criteria:
  - stacks/_base.json exists and contains valid JSON conforming to StackModule schema
  - detect.files is ['*'] with detect.match 'any'
  - "permissions.allow contains at least: ls, cat, head, tail, wc, find, grep, echo, which, mkdir, cp, mv, touch, chmod, diff, sort, uniq, xargs, tr, cut, tee, date, pwd, basename, dirname, realpath, env (all as Bash(cmd:*) format)"
  - permissions.allow contains 'Read' (unqualified, allows all file reads)
  - "permissions.deny contains at least: rm -rf /, rm -rf ~, sudo, curl piped to sh, wget piped to sh"
  - "permissions.deny contains: Edit(//.env), Edit(//.*credentials*), Edit(//.ssh/**)"
  - sandbox.filesystem.allowWrite is ['.'] and sandbox.filesystem.denyWrite is ['/.git']
module: null
domain: null
notes: |
  # 002: Create _base.json stack with universal defaults and deny list
  
  ## Implementation Notes
  
  `_base.json` is the foundation stack — always included regardless of detection results. It serves two purposes:
  
  1. **Universal allow rules** for read-only and non-destructive CLI tools that every project needs
  2. **Safety deny rules** that block known-dangerous command patterns
  
  ### Allow rules (Bash format)
  
  Common read-only tools:
  - `ls`, `cat`, `head`, `tail`, `wc`, `find`, `grep`, `echo`, `which`, `pwd`, `date`
  
  Non-destructive file operations:
  - `mkdir`, `cp`, `mv`, `touch`, `chmod`, `diff`
  
  Text processing:
  - `sort`, `uniq`, `xargs`, `tr`, `cut`, `tee`, `sed`, `awk`
  
  Path utilities:
  - `basename`, `dirname`, `realpath`
  
  Environment:
  - `env`, `export`, `printenv`
  
  File reading (unqualified):
  - `Read` — allows all file reads without path restrictions
  
  ### Deny rules
  
  Destructive filesystem operations:
  - `Bash(rm -rf /:*)` — recursive delete from root
  - `Bash(rm -rf ~:*)` — recursive delete from home
  - `Bash(rm -rf /*:*)` — recursive delete from root (alternate form)
  
  Privilege escalation:
  - `Bash(sudo:*)` — all sudo commands
  
  Remote code execution:
  - `Bash(curl * | sh:*)` — pipe curl to shell
  - `Bash(curl * | bash:*)` — pipe curl to bash
  - `Bash(wget * | sh:*)` — pipe wget to shell
  - `Bash(wget * | bash:*)` — pipe wget to bash
  
  Sensitive file edits:
  - `Edit(//.env)` — environment files with secrets
  - `Edit(//.*credentials*)` — credential files
  - `Edit(//.ssh/**)` — SSH keys and config
  
  ### Sandbox defaults
  
  ```json
  "sandbox": {
    "filesystem": {
      "allowWrite": ["."],
      "denyWrite": ["/.git"]
    }
  }
  ```
  
  ### Important: detect block
  
  Since `_base` is always included, its detect block should match everything:
  ```json
  "detect": {
    "files": ["*"],
    "match": "any"
  }
  ```
cycle_created: 1
cycle_modified: null
content_hash: "06b005cac576f15b6945a82159d3764ddde28a688a53be7bb86235ab048fa747"
token_count: 730
file_path: .ideate/work-items/WI-002.yaml
